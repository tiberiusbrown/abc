{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Arduboy File",
            "type": "shell",
            "command": "abcc main.abc -b fxdata.bin -u -a DazzleDash.arduboy",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
