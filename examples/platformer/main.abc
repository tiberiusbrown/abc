import map;
import update;
import render;
import vars;

void setup()
{
    $set_frame_rate(50);
    load_map(0);
}

void loop()
{
    while(!$next_frame())
        ;
    
    update();
    render();
    
    $display();
}

void main()
{
    setup();
    while(true)
        loop();
}
