import map;
import update;
import render;
import vars;

void setup()
{
    $set_frame_rate(50);
    current_map = 0;
    camy = 64;
    px = 30;
    py = 80;
    player_facing_right = true;
}

void loop()
{
    while(!$next_frame())
        ;
    
    update();
    render();
    
    $display();
}

void main()
{
    setup();
    while(true)
        loop();
}
