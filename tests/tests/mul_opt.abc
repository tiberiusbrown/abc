u8[4] a = { 1, 2, 3, 4 };

void main()
{
    $debug_break();
    
    $assert(u16(a[1] + a[2]*256) == 0x302);
    $assert(u24(a[1] + a[2]*(1<<16)) == 0x30002);
    $assert(u32(a[1] + a[2]*(1<<24)) == 0x3000002);
    
    $debug_break();
}
