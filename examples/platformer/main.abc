import map;
import update;
import render;
import sounds;
import vars;

void setup()
{
    $set_frame_rate(50);
    load_map(0);
    px = 197.58;
    py = 57.75;
}

void loop()
{
    while(!$next_frame())
        ;
    
    update();
    render();
    
    $display();
}

void main()
{
    setup();
    while(true)
        loop();
}
